// When I click AM/PM color of container need to switch
    // Everytime I click the switch container, the button colors for AM/PM need to change

const btnSwitch = document.getElementById("switch")

btnSwitch.addEventListener("click", colorToggle) 

function colorToggle() {
    alert("Clicked")
}